# negot_strategy_core_pack

This bundle contains:
- `docs/STRATEGY_SYSTEM.md` — documentation
- `schemas/` — JSON Schemas
- `strategy_packs/core/` — CORE pack with 30 strategies + 2 rubrics

Use this as a drop-in `strategy_packs/` + `schemas/` folder in your repo.
